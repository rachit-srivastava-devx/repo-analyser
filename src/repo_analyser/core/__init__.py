"""Shared primitives with no internal dependencies: subprocess execution,
repo discovery, CSV/JSON I/O, and language detection. Every other subpackage
depends on this one; this one depends on nothing else in the package.
"""
