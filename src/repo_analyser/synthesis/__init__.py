"""Cross-cutting analysis computed over multiple collectors' output: the
composite risk ranking (synthesize), the per-category deep reports
(deep_reports), and the capstone briefing spec (exec_deck). Nothing here
calls an external tool directly -- it reads other modules' CSV/JSON.
"""
